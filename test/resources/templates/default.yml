http:
  routers:
    magic-proxy-{{ app_name }}:
      rule: Host(`{{ app_name }}.{{ hostname }}`)
      service: magic-proxy-{{ app_name }}
      entryPoints:
        - web
  services:
    magic-proxy-{{ app_name }}:
      loadBalancer:
        servers:
          - url: "{{ target_url }}"
